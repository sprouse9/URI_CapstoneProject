path: archive # Root directory of your dataset
train: train/images  # Training images folder (relative to 'path')
val: val/images  # Validation images folder (relative to 'path')

# Class names (adjust if you have different classes)
names:
  0: car
